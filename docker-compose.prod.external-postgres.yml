version: '3.8'

services:
  api:
    environment:
      SNIPPETS_API_BACKEND: ${SNIPPETS_API_BACKEND:-database}
      POSTGRES_HOST: ${POSTGRES_HOST:-shared-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_DB: ${POSTGRES_DB:-snippets}
      POSTGRES_USER: ${POSTGRES_USER:-snippets}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-snippets}
      POSTGRES_SSL: ${POSTGRES_SSL:-false}
    networks:
      - default
      - shared-postgres

networks:
  shared-postgres:
    external: true
    name: ${SHARED_POSTGRES_NETWORK:-shared-services}
